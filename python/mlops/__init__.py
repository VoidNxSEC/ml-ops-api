"""
MLOps package — migrated from neotron/neutron ML layer.

Provides:
- mlops.models         — core ML dataclasses
- mlops.optimization   — hyperparameter optimization
- mlops.tracking       — cost/experiment tracking
- mlops.plugins        — semantic optimizers (Cerebro)
- mlops.reasoning      — ensemble reasoning + DSPy adapter
- mlops.llm            — LLM client with fallback chain
"""
